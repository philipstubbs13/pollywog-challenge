import { makeStyles } from '@material-ui/core/styles';

export const useUiFooterStyles = makeStyles({
  card: {
    width: '100%',
    marginTop: 50,
    height: '10rem',
  },
  title: {
    fontSize: 14,
  },
  pos: {
    marginBottom: 12,
  },
  footerContent: {
    margin: 10,
  },
});
